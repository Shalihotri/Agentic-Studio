{
  "buildCommand": "cd frontend && npm install && npm run build && cp -r dist ../app/static",
  "builds": [
    {
      "src": "app/main.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app/main.py"
    }
  ]
}
