{
  "buildCommand": "npm install --prefix frontend && npm run build --prefix frontend && cp -r frontend/dist app/static",
  "builds": [
    {
      "src": "app/main.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app/main.py"
    }
  ]
}